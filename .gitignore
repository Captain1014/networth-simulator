.DS_Store
*.log
node_modules/
input-evaluation.md
i18n-guide.md

# --- Personal / secrets (never commit) ---
scenario-import.json
*-personal.json
*-private.json
.env
.env.*
!.env.example
*.pem
id_rsa
id_rsa.*
*.key

# Optional: keep a local-only backup script with real ciphertext (use instead of editing backup.js)
js/backup.local.js

# --- Local tool / agent state (not part of the app) ---
.claude/
.omc/
.bkit/
