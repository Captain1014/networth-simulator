// Default: no embedded backup. Copy to backup.js and paste ciphertext from the app, or leave as-is.
// Loaded before backup.js so local backup.js can override (see .gitignore).
var ENCRYPTED_BACKUP = '';
